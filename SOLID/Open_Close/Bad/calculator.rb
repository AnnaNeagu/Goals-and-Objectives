class AreaCalculator
    shapes = []  # area with all shapes
    # information about shapes
    def initialize(shapes)
        @shapes = shapes
    end

    def sum
        if shapes == "square"
            area = pow(lenght,2)
        elsif shapes = "rectangle"
            area =  width * lenght
        elsif ...
            # algorithm for calculate area 
            # this code violate the open close principle because has many if else blocks
                
            end
        end

    end

end